# TR-109 — Alt-arrow pane navigation

1. Remove native Tab, Ctrl+I, and Shift+Tab focus bindings.
2. Bind `M-Left` directly to the LWA pane.
3. Bind `M-Right` directly to the Codex pane.
4. Retain cleanup for legacy bindings from prior LWA launches.
5. Run focused tests, lint, and the full suite.
6. Manually verify Alt+Left/Right in Ghostty/tmux and prompt Tab behavior.

